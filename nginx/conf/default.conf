server {
    listen 80;
    server_name https://resume.thaisalepage.com;
    index index.php index.html;
    error_log /var/log/nginx/error.log;
    access_log /var/log/nginx/access.log;
    root /var/www/public;
    location ~ \.php$ {
        try_files $uri =404;
        fastcgi_split_path_info ^(.+\.php)(/.+)$;
        fastcgi_pass web:9000;
        fastcgi_index index.php;
        include fastcgi_params;
        fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
        fastcgi_param PATH_INFO $fastcgi_path_info;
    }
    location / {
        try_files $uri $uri/ /index.php?$query_string;
        gzip_static on;
    }
}

# server {
#     listen 80;
#     server_name resume.thaisalepage.com;  # ระบุโดเมนที่ต้องการให้ใช้งาน
#     index index.php index.html;
#     error_log /var/log/nginx/error.log;
#     access_log /var/log/nginx/access.log;
#     root /var/www/public;

#     location ~ ^/delete/experience/.* {
#         allow 127.0.0.1;  # กำหนด IP ที่อนุญาตให้ใช้ DELETE
#         deny all;  # ปฏิเสธการเข้าถึงที่ไม่ได้ระบุไว้
#         try_files $uri $uri/ /index.php?$query_string;
#     }

#     location / {
#         try_files $uri $uri/ /index.php?$query_string;
#         gzip_static on;
#     }

#     location ~ \.php$ {
#         try_files $uri =404;
#         fastcgi_split_path_info ^(.+\.php)(/.+)$;
#         fastcgi_pass web:9000;
#         fastcgi_index index.php;
#         include fastcgi_params;
#         fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
#         fastcgi_param PATH_INFO $fastcgi_path_info;
#     }
# }